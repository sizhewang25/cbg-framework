# CBG vs Shortest-Ping — Identify Bad-Prediction Targets — Report

**Status**: In Progress
**Created**: 2026-05-26
**Last Updated**: 2026-05-26

## Summary

To be filled as work progresses. Goal: enumerate the eval anchors for which
each CBG variant predicts worse than the shortest-ping baseline on the
`north_america_as7018` run (AT&T probe corpus → 5-fold-stratified anchor
eval set).

## Findings

### Baseline definitions
- Shortest-ping prediction for target T: VP coord of `argmin_{vp ∈ obs(T)} latency_ms`.
- Baseline error: `haversine(true_target_coord, nearest_vp_coord)`.
- CBG error: `error_km` column in each combo's `targets.parquet`.
- Delta of interest: `delta_km = error_CBG − error_baseline`.

### Layout verified
- Outputs: `scripts/benchmark/v2/outputs/north_america_as7018/ripe_atlas_asn_corpora/probes_to_anchors/fold_{0..4}/{vanilla_cbg,million_scale_cbg,octant_cbg,spotter_cbg}/{targets.parquet, fit_checkpoint.pkl, run.json}`
- Inputs (per fold): `inputs/ripe_atlas_asn_corpora/north_america_as7018/probes_to_anchors/fold_{0..4}/eval_observations.parquet` (required for the baseline computation; not regenerated by this task — assumes the original materialize-inputs run is on disk)

### Numbers
*(populated after Phase 1)*

| Combo | n_total | n_CBG_better | n_baseline_better | median Δ (km) |
|---|---|---|---|---|
| vanilla_cbg | — | — | — | — |
| million_scale_cbg | — | — | — | — |
| octant_cbg | — | — | — | — |
| spotter_cbg | — | — | — | — |

### Worst-N targets
*(populated after Phase 1)*

### Always-bad intersection
*(populated after Phase 1)*

## Conclusions

To be written when Phase 1 + Phase 3 complete.
