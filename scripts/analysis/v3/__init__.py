"""v3 analysis layer over `outputs/benchmark/v2/`.

Library + thin CLI. Every module — libraries and CLI command bodies alike —
lives under `modules/`; `cli.py` only imports and registers them.

See SCHEMA.md for the v2 output-schema reference this layer reads against.
"""
